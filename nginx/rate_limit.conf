map $request_method $login_req_key {
    POST    $binary_remote_addr;
    default "";
}
limit_req_zone $login_req_key zone=login_limit:10m rate=5r/m;

map $request_method $content_req_key {
    POST    $binary_remote_addr;
    default "";
}
limit_req_zone $content_req_key zone=content_limit:10m rate=6r/m;

map $request_method $admin_req_key {
    POST    $binary_remote_addr;
    default "";
}
limit_req_zone $admin_req_key zone=admin_limit:10m rate=20r/m;
