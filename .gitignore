# Окружение и секреты
.env
*.db

# Внутренние отчёты аудита (не для публичного репо)
docs/audit-*.md
docs/production-switch.md

# Скрипты с захардкоженными учётными данными или временные
deploy_sftp.py
scripts/_*.py
scripts/get_vk_token.py
scripts/test_vk_photo.py

# Внутренние документы — не для публичного репо
SECURITY_AUDIT_*.md
SSH_ACCESS.md
domik-putty-key/

# PostgreSQL TLS сертификаты (хранятся только на сервере)
pg-certs/

# Claude Code — содержит учётные данные сервера
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
/python

# Загруженные файлы (хранятся на сервере, не в git)
uploads/*
!uploads/.gitkeep

# Логи (хранятся на сервере, не в git)
logs/*.jsonl

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
